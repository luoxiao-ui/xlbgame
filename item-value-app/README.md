# Item Value App (Android Native)

这是一个 Android 原生估值 App（Kotlin + XML），不再依赖 JS/HTML/CSS。

## 打开方式

1. 用 Android Studio 打开目录：`item-value-app`
2. 等待 Gradle Sync 完成
3. 运行 `app` 模块到模拟器或真机

## 功能

- 按品类、价格、购买日期、成色、配件、维修史、热度、稀缺度和趋势估值
- 输出建议成交价、回收价、基准估值、可信度、影响因素
